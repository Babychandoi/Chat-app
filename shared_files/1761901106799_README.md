# 📘 Schedule Management System

Hệ thống quản lý lịch học được xây dựng bằng **Spring Boot (Backend)** và **React (Frontend)**.

---

## ⚙️ 1. Cấu trúc dự án

```
ScheduleManagement/
│
├── backend/                # Spring Boot project (Java)
│   ├── src/
│   ├── pom.xml
│   └── ...
│
└── frontend/               # React project (TypeScript/JavaScript)
    ├── src/
    ├── package.json
    └── ...
```

---

## 🚀 2. Chạy Backend (Spring Boot)

### 🔧 Yêu cầu:
- Cài đặt **Java 17+**
- Cài đặt **Maven 3.8+**
- Đã cấu hình file `application.properties` hoặc `application.yml` để kết nối đúng database.

### ▶️ Cách chạy:
Mở terminal trong thư mục `backend`:
```bash
cd backend
mvn clean install
mvn spring-boot:run
```

Hoặc nếu đã build ra file `.jar`, có thể chạy:
```bash
java -jar target/schedulemanagement-0.0.1-SNAPSHOT.jar
```

> 🟢 Mặc định backend chạy tại:  
> **http://localhost:8080**

---

## 💻 3. Chạy Frontend (React)

### 🔧 Yêu cầu:
- Cài đặt **Node.js** (phiên bản 18 trở lên)
- Cài đặt **npm** hoặc **yarn**

### ▶️ Cách chạy:
Mở terminal trong thư mục `frontend`:
```bash
cd frontend
npm install     # Cài đặt các dependencies
npm start       # hoặc yarn start
```

> 🟢 Mặc định frontend chạy tại:  
> **http://localhost:3000**

---

## 🔄 4. Cấu hình CORS cho Backend

Đảm bảo trong cấu hình CORS của Spring Boot, có cho phép domain frontend truy cập:
```java
configuration.setAllowedOrigins(List.of(
    "http://localhost:3000",
    "https://tên-miền-deploy-frontend.com"
));
```

---

## 🧠 5. Một số lệnh Git cơ bản

```bash
git add .                          # Thêm tất cả file thay đổi
git commit -m "Cập nhật tính năng mới"
git push                           # Đẩy code lên GitHub
```

---

## 🧑‍💻 6. Công nghệ sử dụng

### 🔹 Backend:
- Spring Boot  
- Spring Security  
- JWT Authentication  
- MySQL  
- Maven  

### 🔹 Frontend:
- React  
- TypeScript  
- Material UI  
- Axios  

### 🔹 Triển khai:
- Ngrok (tạm thời test HTTPS)
- Render / Vercel (deploy chính thức)

---

## 📦 7. Cấu hình database ví dụ (application.properties)

```properties
spring.datasource.url=jdbc:mysql://localhost:3306/schedule_db
spring.datasource.username=root
spring.datasource.password=123456
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
```

---

## 🪄 8. Hướng dẫn deploy (tùy chọn)

### ✅ Deploy backend bằng Render:
1. Push code backend lên GitHub.  
2. Truy cập [https://render.com](https://render.com).  
3. Chọn “New +” → “Web Service”.  
4. Kết nối GitHub repo backend.  
5. Thiết lập:
   - Build command: `mvn clean package -DskipTests`
   - Start command: `java -jar target/<tên-file>.jar`
6. Render sẽ tự động build và chạy Spring Boot.

### ✅ Deploy frontend bằng Vercel:
1. Push code frontend lên GitHub.  
2. Truy cập [https://vercel.com](https://vercel.com).  
3. Import repo frontend → Deploy.  

---

## 📜 9. Giấy phép

© 2025 **Schedule Management System** — Developed by *Đỗ Quốc Phong*.
